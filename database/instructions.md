1. Navigate into the `database` folder from the root of the repository.
2. Run the command `mysql -u root -p < db.sql` (if you already have data in the database, running this command again will clear it. In that case, run it without "< db.sql")
3. Enter the sudo password of the computer
