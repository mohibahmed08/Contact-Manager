document.addEventListener("DOMContentLoaded", () => {
    console.log("Contact Edit page loaded");

    // Example: grab elements
    const initials = document.getElementById("edit-contact-initials");
});
